from setuptools import find_packages, setup

setup(
    name="worker",
    version="0.0.1",
    description="Health check Worker",
    license="MIT",
    packages=find_packages(),
    install_requires=[],
)
