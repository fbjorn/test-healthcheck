from setuptools import find_packages, setup

setup(
    name="backend",
    version="0.0.1",
    description="Health check Backend",
    license="MIT",
    packages=find_packages(),
    install_requires=[],
)
